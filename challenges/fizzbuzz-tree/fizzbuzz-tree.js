module.exports = function fizzBuzzTree(tree) {
  // if node value is divisible by 3, node value = Fizz
  // if node value is divisible by 5, node value = Buzz
  // if node value is divisible by 3 and 5, node value = FizzBuzz
  return tree;
};
